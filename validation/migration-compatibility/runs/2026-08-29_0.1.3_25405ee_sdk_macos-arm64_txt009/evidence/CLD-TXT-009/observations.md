# CLD-TXT-009 Observations

- Focused test passes and asserts SDK `query` receives the selected `effort` while
  thinking remains adaptive/summarized.
- Visible selector was explicitly changed from Sonnet Medium to Sonnet High before send.
- Link config records `effort: high`; native Claude assistant event on the same exact
  Session ID independently records top-level `effort: high` and model `claude-sonnet-5`.
- DSH archive final is exact, source model is Sonnet, tools are zero, and Workspace is
  unchanged. Screenshot SHA-256 is
  `a0165a44b9a0cb4a8cd0615d87e147fe902092dac988526970d6a0bb4cc82eb3`.
