# Self-review

## Process validity

- Positive controls independently prove both extension types are available and executable in this exact runtime.
- Denied branches differ only by selected project source containing exact native tool deny rules.
- Callback count and child transcript are execution oracles stronger than presentation/tool-list inference.

## Result reliability

- MCP deny retains Relay's explicit allowlist yet produces zero callback, decisively proving deny precedence.
- Agent control has a hashed real child transcript; denied branch has zero Agent and no denied child marker.
- Duplicate MCP failed projections have one tool ID and zero callback, so they are not duplicate execution.

## Verdict

Pass. Effective project rules prevent both DSH MCP and Agent execution; extension paths do not bypass deny.
