# CLD-EXT-007 Observations

- Tool-workspace native deferred metadata adds only the exact project MCP tool; one exact selector,
  allow-once business call, server log, native result/attribution, DSH output, and final all agree.
- The sibling initial deferred list omits the project tool. Its identical ToolSearch selector returns
  `matches: []` and `No matching deferred tools found`, with zero approval or business tool.
- The append-only server log remains at five lines across the entire sibling trial: two positive
  process/connect pairs and one positive business call. No sibling process is started.
- No root user or sibling project fixture config exists. Nested Git and attachment object state are
  stable. The temporary owner `.mcp.json` is removed and all fixture processes have exited.
- Selecting the sibling after opening a new composer creates an empty source-Workspace DSH shell;
  it contains no Claude/link/tool state and does not affect the two business trials.
