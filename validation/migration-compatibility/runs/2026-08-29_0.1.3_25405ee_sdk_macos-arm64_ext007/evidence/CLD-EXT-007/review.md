# CLD-EXT-007 Validation Review

## Reasonableness

- A positive call alone would not prove project scope. The contemporaneous sibling uses the exact
  same selector while the owner config remains installed; zero match and zero process delta are a
  direct negative control.
- Independent tool names, source/result markers, append-only process oracle, native deferred lists,
  and DSH lifecycles distinguish configuration scoping from a model claim.

## Reliability

- Positive evidence agrees across config, server, native, DSH, and UI. Negative evidence agrees
  across sibling deferred metadata, exact ToolSearch structured result, DSH, UI, and unchanged log.
- The empty source shell is attributable to pre-send Workspace switching and has no model binding;
  excluding it cannot conceal a tool exposure or server process. Cleanup/source/state checks pass.

## Verdict

**Pass, high confidence.** Project MCP executes in its owning Workspace and is absent from the
sibling Workspace under an exact, independently observed scope test.
