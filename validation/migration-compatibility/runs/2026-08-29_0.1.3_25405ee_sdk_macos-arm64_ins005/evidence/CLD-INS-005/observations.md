# Observations

- Configured root file contains only the relative import and no answer marker.
- Configured final is the exact marker found only in imported.md; sibling says no instruction was shared
  and has zero marker occurrence. Both use no tools and complete without diagnostic.
