# Observations

- With config true, the unrelated fresh init has 14 Skills and the fixture Skill/command/Agent namespaces.
- CLI disable changes only the plugin boolean while inventory still reports the same installed 1.0.4.
- The next fresh init has 12 control Skills and zero fixture namespace occurrences.
- Both prompts use no tools and exact finals, so discovery is initialization-driven rather than invocation.
- Cleanup restores settings/marketplace SHA, empty inventory, absent registry and absent fixture cache.
