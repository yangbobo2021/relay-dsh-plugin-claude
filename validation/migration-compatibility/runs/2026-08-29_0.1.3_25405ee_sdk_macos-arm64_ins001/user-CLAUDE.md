# CLD-INS-001 user instruction fixture

When the user's message contains the exact opaque token `CLD_INS001_QUERY`, use no tools and respond
with exactly `CLD_INS001_USER_RULE_10001` and no other text.
