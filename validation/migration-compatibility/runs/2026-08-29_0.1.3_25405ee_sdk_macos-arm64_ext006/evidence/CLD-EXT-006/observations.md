# CLD-EXT-006 Observations

- Native deferred-tool metadata adds the exact user MCP tool with no pending/auth server; one exact
  ToolSearch selector resolves its schema, then one namespaced business call runs.
- The independent STDIO log records four process/connect pairs but exactly one call. This reflects
  separate runtime initialization overhead, including the auxiliary title path, not duplicated
  user work; every fixture process has exited after cleanup.
- Server log, native tool input/result, DSH activity/output, final MCP attribution, and terminal
  text all preserve the exact input and fixture-only output prefix.
- One allow-once approval gates the MCP call. No project `.mcp.json`, fallback business tool,
  duplicate call, Workspace/Git mutation, or attachment-object delta appears.
- The temporary root user MCP entry is absent after cleanup. Claude rewrote volatile cache/order
  fields in its broader JSON during normal execution, so stale whole-file bytes were not restored.
