# Observations

- Host PATH does not include the fixture bin; both Sessions execute the same one-word command once.
- With project source the executable returns exact stdout; without it Bash reports exit 127/command not found.
- Both calls required and received one equivalent approval, excluding permission as the differential.
- No fallback or diagnostic occurred and the formerly absent project settings are absent again.
