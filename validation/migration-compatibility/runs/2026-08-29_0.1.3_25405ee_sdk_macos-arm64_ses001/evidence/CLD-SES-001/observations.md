# Observations

- New DSH pages default to Standard/DeepSeek, so migration users must explicitly select Claude Code; the invalid
  shell failed pre-Claude and created no link.
- The valid Session visibly completes one zero-tool turn and adds one DSH→Claude business mapping.
- Native title generation creates a second Claude Session containing the prompt, but it is absent from link store;
  only the business Session owns continuation.
- DSH archive, native transcript, link config and visible final agree on Workspace/model/marker.
