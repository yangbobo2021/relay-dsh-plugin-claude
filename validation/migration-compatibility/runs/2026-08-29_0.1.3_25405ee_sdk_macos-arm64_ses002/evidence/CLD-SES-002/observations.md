# Observations

- Full browser reload automatically reselects the DSH Session and renders its first user/final pair.
- Follow-up contains no old token, yet exact prior token recall and current marker appear in one final.
- Link file is byte-identical; the same mapped native transcript grows to two user/two assistant records.
- DSH archive grows from one to two turn boundaries; no tool row appears in either turn.
