# CLD-EXT-002 Observations

- Tool-workspace business initial listing has 14 skills, including exact user and project fixtures;
  its cwd is the project fixture root and the unrelated probe uses zero tools.
- Post-install sibling business listing has 13 skills, retains the user fixture, and excludes the
  project fixture/name/description; cwd is the sibling root and it also uses zero tools.
- Auxiliary title Sessions consistently use user-only settings and therefore provide a useful
  control but are not treated as project-positive evidence.
- Both DSH turns complete, no object appears, and UI selection is restored to tool-workspace.
