# Self-review

## Process validity

- Same project/model/source/query; only accessed path enters the nested instruction directory.
- No root CLAUDE.md or expected marker in prompt/target files can confound the differential.

## Result reliability

- Exact Read paths/results plus marker-only nested final and zero marker control prove directory-triggered
  loading. Native hashes and no mutation preserve reproducibility.

## Verdict

Pass. Nested CLAUDE.md loads when its directory is accessed and does not apply to root-only work.
