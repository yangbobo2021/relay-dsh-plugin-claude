# Observations

- Nested branch reads only nested target and then returns exact unprompted nested marker.
- Root control reads only root-control and contains zero nested marker occurrence.
- Both complete one Read with exact content, no approval/diagnostic or file mutation.
