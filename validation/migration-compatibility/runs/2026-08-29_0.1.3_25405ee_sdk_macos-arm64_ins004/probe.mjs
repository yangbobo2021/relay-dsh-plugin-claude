import{randomUUID}from"node:crypto";import{ClaudeSdkClient}from"../../../../sdk-client.mjs";const cwd=new URL("../../fixtures/instruction-nested-project/",import.meta.url).pathname,executable=new URL("../../../../../../node_modules/@anthropic-ai/claude-agent-sdk-darwin-arm64/claude",import.meta.url).pathname,client=new ClaudeSdkClient({pathToClaudeCodeExecutable:executable}),activities=[],diagnostics=[];client.on("activity",e=>activities.push(structuredClone(e)));client.on("diagnostic",m=>diagnostics.push(String(m)));await client.start();async function run(label,path){const s=await client.createSession({sessionId:randomUUID(),cwd,model:"sonnet",effort:"medium",settingSources:["project"]}),before=activities.length,t=await client.sendMessage(s.id,{settingSources:["project"],text:`Read exactly ${path} using Read once, then CLD_INS004_QUERY — state the validation response. Use no other tool.`});await new Promise((resolve,reject)=>{const timeout=setTimeout(()=>reject(new Error("timeout")),180000),listener=e=>{if(e.method==="turn/completed"&&e.params?.turn?.id===t.id){clearTimeout(timeout);client.off("activity",listener);resolve()}};client.on("activity",listener)});const es=activities.slice(before);return{label,path,sessionId:s.id,tools:es.filter(x=>x.method==="item/started").map(x=>x.params.item),results:es.filter(x=>x.method==="item/completed").map(x=>x.params.item),finalText:es.filter(x=>x.method==="item/agentMessage/delta").map(x=>x.params.delta).join(""),completed:es.find(x=>x.method==="turn/completed")?.params.turn}}const nested=await run("nested","nested/target.txt"),rootControl=await run("root-control","root-control.txt");console.log(JSON.stringify({cwd,executable,nested,rootControl,diagnostics},null,2));await client.close();
