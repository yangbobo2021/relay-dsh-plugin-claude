# Observations

- Relay passes exactly `[user]`, `[user,project]`, and `[user,project,local]` in the same cwd.
- One identical Bash env read returns USER_4004, PROJECT_4004, and LOCAL_4004 respectively.
- Each branch has one accepted unchanged-input approval, one completed call and one completed turn.
- No diagnostic or mutation occurs; real user settings return to their original bytes/SHA.
