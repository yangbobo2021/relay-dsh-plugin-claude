# Observations

- Exact old PID stopped gracefully and 4394 closed before a distinct new PID became healthy.
- Same DSH home and Relay link path restore selected Session, two turns, Claude preset/model and composer.
- Third prompt omits the prior token; exact recall proves resumed Claude context after process-memory loss.
- Link bytes remain unchanged; its native/DSH artifacts each add exactly one completed user/assistant turn.
