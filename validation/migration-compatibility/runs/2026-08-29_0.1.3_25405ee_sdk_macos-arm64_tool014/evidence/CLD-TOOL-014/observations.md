# CLD-TOOL-014 Observations

- Native Claude uses exactly ToolSearch then AskUserQuestion with the requested schema and options.
- DSH shows a waiting-for-answer Session and interactive radio card with no final. BETA becomes
  checked, Submit becomes enabled, and one submission removes the card.
- Native structured result maps the exact question to `BETA_1414`; DSH tool output and exact final
  agree. The same DSH/Claude business binding persists throughout.
- Workspace, nested Git, and object state remain unchanged.
