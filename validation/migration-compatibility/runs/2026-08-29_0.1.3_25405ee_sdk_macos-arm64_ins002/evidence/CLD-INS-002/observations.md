# Observations

- Both queries pass project-only source and their exact sibling cwd.
- Configured project returns the unprompted rule marker; sibling returns `No response.` with zero marker.
- Both complete with zero tools and diagnostics; user instruction file remains absent.
