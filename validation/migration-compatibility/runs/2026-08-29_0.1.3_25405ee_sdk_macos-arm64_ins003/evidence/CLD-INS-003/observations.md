# Observations

- Matching branch reads exactly `src/target.txt`, gets its exact content, then returns rule-only marker.
- Nonmatching branch reads exactly `docs/other.txt`, gets control content, and has zero rule-marker occurrence.
- Both have one Read, no other tool/approval/diagnostic, completed turns and unchanged files.
