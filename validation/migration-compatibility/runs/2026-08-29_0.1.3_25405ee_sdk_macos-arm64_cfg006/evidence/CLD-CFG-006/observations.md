# Observations

- Adapter creation and send records agree on every DSH-owned field and retain all setting sources.
- Real query options explicitly contain Sonnet, Medium, default permission and exact fixture cwd.
- Native Claude records `claude-sonnet-5`, Medium, default permission and the same normalized cwd despite
  loaded project settings requesting Haiku, Low and Plan.
- No tool, approval, diagnostic or mutation occurs.
