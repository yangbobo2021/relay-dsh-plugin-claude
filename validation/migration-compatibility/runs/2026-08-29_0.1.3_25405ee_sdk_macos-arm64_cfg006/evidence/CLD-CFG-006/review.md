# Self-review

## Process validity

- The component probe tests DSH event/header/options mapping in the production adapter; the live probe
  tests real Claude setting collision and actual native outcome. Neither layer alone is overclaimed.
- Project setting source stays enabled, so the result is precedence rather than settings suppression.
- The failed recorder clone attempt never completed and is excluded after a minimal observer-only fix.

## Result reliability

- Query options and native records independently agree for model, effort, permission mode and cwd.
- Conflicting fixture settings are immutable and hashed. Exact native values, not prompt text, prove wins.
- Source hashes and no-tool state rule out fallback or mutation.

## Verdict

Pass. DSH/Relay owns the four documented execution fields while other Claude settings remain enabled.
