# CLD-EXT-001 Observations

- A prior fresh Session lists 12 skills and not the unique fixture. After global installation,
  both new business and auxiliary native Sessions have initial 13-skill listings containing the
  exact fixture name and description.
- Global and canonical SKILL/reference/script bytes match exactly. The selected tool Workspace has
  no project copy, isolating discovery to the user setting source.
- The unrelated probe completes with exact text and zero tools, so invocation or prompt echo cannot
  manufacture the listing evidence. Workspace/Git/object state remains stable.
