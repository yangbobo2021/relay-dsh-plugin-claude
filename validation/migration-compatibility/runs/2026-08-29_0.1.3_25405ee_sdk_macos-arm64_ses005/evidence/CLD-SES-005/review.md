# Self-review

## Process validity

- The first trial was correctly rejected because PreCompact alone is not proof of compaction.
- Valid trial requires PostCompact plus a native boundary and numeric token reduction, eliminating ordinary memory.
- Recall prompt omits the marker and uses the same native Session after the compact boundary.

## Result reliability

- Two independent hook processes and immutable native JSONL agree on trigger, Session, cwd and summary.
- Token drop is material (60,050), and summary record/hash proves the early identifier is retained.
- Exact recall, zero tools/diagnostics and cleanup complete the end-to-end supported path.

## Verdict

Pass. Relay's Claude SDK Session survives real context compaction and retains an important early marker.
