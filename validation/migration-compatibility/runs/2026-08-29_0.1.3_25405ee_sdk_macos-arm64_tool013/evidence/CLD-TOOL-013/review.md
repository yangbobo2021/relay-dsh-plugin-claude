# CLD-TOOL-013 Validation Review

## Reasonableness

- Independent HTTP calibration provides a live oracle. Requiring deferred-tool discovery plus an
  actual WebFetch result prevents the well-known heading from passing by model memory.
- Explicitly forbidding alternate network paths isolates the migrated built-in capability.

## Reliability

- Calibrated HTTP 200/body heading, exact ToolSearch reference, structured WebFetch code/bytes/url/
  result, one approval, DSH lifecycles, exact final, normal turn completion, and stable state agree.

## Verdict

**Pass, high confidence; supported.** WebFetch is discoverable and can read a fixed public source
with explicit approval and correct attribution.
