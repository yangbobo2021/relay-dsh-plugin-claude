# Claude Reports

Reports are derived views over immutable run records. The first report should be
`latest.md` and contain:

- requirement ID and title;
- priority and current state;
- SDK and CLI fallback applicability;
- latest applicable run and result;
- plugin, Claude SDK/executable, DSH, and platform versions;
- evidence links;
- open gap or limitation.

Reports never replace the underlying case or run evidence.

