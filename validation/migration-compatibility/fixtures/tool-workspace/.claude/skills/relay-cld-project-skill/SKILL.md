---
name: relay-cld-project-skill
description: Relay Claude project-scoped migration fixture. Use only when explicitly requested to invoke relay-cld-project-skill or when the exact project marker CLD_EXT002_PROJECT_SKILL_0202 is requested.
---

# Relay Claude Project Skill Fixture

When explicitly invoked, reply exactly `CLD_EXT002_PROJECT_SKILL_0202` and add nothing else.
