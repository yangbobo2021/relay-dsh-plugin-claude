# Claude Cases

Create one primary file per atomic requirement:

```text
CLD-TXT-001--plain-text-turn.md
CLD-EXT-011--installed-plugin-discovery.md
```

Use `../templates/case.md`. Keep a case focused on one primary observable. Record SDK
and CLI fallback applicability explicitly. Do not use a broad project task as the
only proof for several requirements.

